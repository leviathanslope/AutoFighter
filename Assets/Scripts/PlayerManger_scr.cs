using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerManger_scr : MonoBehaviour
{

    public int _coin;
    public int _health;
    // Start is called before the first frame update
    void Start()
    {
        _coin = 10;
        _health = 20;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
